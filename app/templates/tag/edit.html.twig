{% include 'default/_form.html.twig' with {
    page_title: 'tag_edit'|trans,
    page_title_param: {'%id%': tag.id},
    attr: {
        method: 'PUT',
        action: url('tag_edit', {'id': tag.id}),
    },
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'tag_index',
} %}
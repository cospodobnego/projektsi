{% include 'default/_form.html.twig' with {
    page_title: 'tag_delete'|trans,
    page_title_param: {'%id%': tag.id},
    attr: {
        method: 'DELETE',
        action: url('tag_delete', {'id': tag.id}),
    },
    form: form,
    submit_label: 'delete'|trans,
    back_to_list_path: 'tag_index',
} %}
{% extends 'base.html.twig' %}

{% block title %}
    {{ 'Category delete'|trans({'%id%': category.id|default('')}) }}
{% endblock %}

{% block body %}
    <div class="category_container container">

    <h1>{{ 'Category delete'|trans({'%id%': category.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('category_delete', {id: category.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('category_index') }}" class="btn btn-link">
            {{ 'Back to list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
    </div>
{% endblock %}
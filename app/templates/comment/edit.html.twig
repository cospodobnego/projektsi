
{% include 'default/_form.html.twig' with {
    page_title: 'title.comment_edit'|trans,
    page_title_param: {'%id%': comment.id},
    attr: {
        method: 'PUT',
        action: url('comment_edit', {'id': comment.id}),
    },
    form: form,
    back_to_list_path: 'post_index',
} %}

{% include 'default/_form.html.twig' with {
    page_title: 'post_edit'|trans,
    page_title_param: {'%id%': post.id},
    attr: {
        method: 'PUT',
        action: url('post_edit', {'id': post.id}),
    },
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'post_index',
} %}
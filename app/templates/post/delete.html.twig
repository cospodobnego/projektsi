
{% include 'default/_form.html.twig' with {
    page_title: 'post_delete'|trans,
    page_title_param: {'%id%': post.id},
    attr: {
        method: 'DELETE',
        action: url('post_delete', {'id': post.id}),
    },
    form: form,
    submit_label: 'delete'|trans,
    back_to_list_path: 'post_index',
} %}